#include <iostream>

using namespace std;


int main (){
	int const size = 10;
	int array[size] = { 1, 3, 5, 7, 5, 8, 64, 5, 34, 87}, value;

	cin >> value;

	for (int i = 0; i < size; i++) {
		if (value == array[i]) {
			return i;
		}
	}
	return -1;

}
